// @flow 
import * as React from 'react';
type Props = {
    
};
const not_found = (props: Props) => {
    return (
        <div>
            Not found
        </div>
    );
};

export default not_found;